const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const count = await prisma.activityLog.count({ where: { action: 'ASSIGN', createdAt: { gte: new Date('2026-04-21') } } }); console.log('Assign actions today:', count); const latest = await prisma.activityLog.findFirst({ where: { action: 'ASSIGN', createdAt: { gte: new Date('2026-04-21') } }, orderBy: { createdAt: 'desc' } }); if (latest) console.log('Latest Assign Action:', latest); } main().finally(() => prisma.$disconnect());
